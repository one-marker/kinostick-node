package kinostick.stream.model.format;

public enum VideoType {
    AVI, //(Audio Video Interleave)
    MKV, //(Matroska)
    MP4, //(MPEG-4 Part 14)
    MPEG, //(Moving Picture Experts Group)
    MOV, //(QuickTime Movie)
    SRT
}
